
.gui
.halt
.end

# dirs
mkdir .vscode bin doc lib inc src tmp

# apt
echo git make curl >> apt.txt

echo code meld doxygen clang-format >> apt.dev
echo g++ gdb                        >> apt.dev
echo flex bison libreadline-dev     >> apt.dev
echo cmake qt5-default              >> apt.dev

# mk

touch README.md Makefile
touch .clang-format .doxygen

# giti
echo '*~'            > .gitignore
echo '*.swp'        >> .gitignore
echo '*.log'        >> .gitignore
echo                >> .gitignore
echo '!.gitignore'  >> .gitignore

echo '*'             > bin/.gitignore
tail -n1 .gitignore >> bin/.gitignore
echo '*.pdf'         > doc/.gitignore
tail -n1 .gitignore >> doc/.gitignore
tail -n1 .gitignore >> lib/.gitignore
tail -n1 .gitignore >> inc/.gitignore
tail -n1 .gitignore >> src/.gitignore
echo '*'             > tmp/.gitignore
tail -n1 .gitignore >> tmp/.gitignore

# src
touch lib/m.ini
touch inc/m.hpp
touch src/m.cpp
touch src/m.lex
touch src/m.yacc
touch CMakeLists.txt

# vscode
touch .vscode/extensions.json
touch .vscode/settings.json
touch .vscode/launch.json
